import React from 'react';

const MyCart = ({ cart }) => {
   // const { cart } = props;
   // console.log(cart);
   return (
      <div>
         {/* <h2>my cart: {cart.length}</h2> */}
      </div>
   );
};

export default MyCart;